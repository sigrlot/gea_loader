table:
  name: tokens
  schema: public
object_relationships:
  - name: count
    using:
      manual_configuration:
        column_mapping:
          denom: denom
        insertion_order: null
        remote_table:
          name: token_count
          schema: public
