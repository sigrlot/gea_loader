注意：本次测试仅为通过性测试，未发现明显TPS性能瓶颈即视为测试通过，不代表该环境下的真实性能数据

数据基础：
- 100万地址，100万区块，100万交易、200万消息
- 所有表全部模拟数据，且均匀关联
测试环境：全本地（i7-14700 8核（超线程）+12核（单线程），32G内存）
数据库配置：
- work_mem=64MB，maintenance_work_mem=1GB，shared_buffers=8GB，effective_cache_size=16GB，wal_buffers=16MB
- max_locks_per_transaction=1024，max_pred_locks_per_transaction=1024，lock_timeout=10s，deadlock_timeout=1s，random_page_cost=1.1
测试场景：用户数 30, 测试时间 60s

测试结果：
/api/block/blocklist    0

